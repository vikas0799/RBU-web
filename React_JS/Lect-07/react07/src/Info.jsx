import React from 'react';

const Info = () => {
    return (
        <div>
            const { firstname } = useParams();
  return <h1>Hello, {firstname}!</h1>;
        </div>
    );
}

export default Info;
